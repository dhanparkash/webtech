export const toggleMenuu = (value) => {
  if (value) {
    return false;
  } else {
    return true;
  }
};

export default function togglesMenu(value) {
  if (value) {
    return false;
  } else {
    return true;
  }
}
